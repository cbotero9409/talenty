<div id="message-<%= message.id %>">
  <small>
    <strong><%= message.user.nickname %></strong>
    <i><%= message.created_at.strftime("%A, %d %B %Y") %></i>
  </small>
  <p><%= message.content %></p>
</div>
